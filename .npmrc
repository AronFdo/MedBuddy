# npm configuration for MedBuddy project
# This file ensures consistent npm behavior across team members

# Enforce engine version requirements strictly
engine-strict=true

# Use package-lock.json for consistent installs
package-lock=true

# Save exact versions (not ranges) for better reproducibility
save-exact=false

# Use npm's default registry
registry=https://registry.npmjs.org/




